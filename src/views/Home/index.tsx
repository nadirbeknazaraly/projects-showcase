import React from 'react';
import { Controls } from './Controls';
import { Container } from 'components';

export const Home = () => {
  return (
    <Container>
      <Controls />
    </Container>
  );
};
