export const HOME = '/';
export const COMPANIES = `${HOME}companies`;
export const REVIEW = `${HOME}review`;
export const DASHBOARD = `${HOME}dashboard`;
export const LOGIN = `${HOME}login`;
export const REGISTER = `${HOME}register`;
export const PASSWORD = `${HOME}password`;

export const ERROR = '/error/';
export const NOT_FOUND = `${ERROR}404`;

export default {
  HOME,
  COMPANIES,
  REVIEW,
  DASHBOARD,
  LOGIN,
  REGISTER,
  PASSWORD,
  ERROR,
  NOT_FOUND,
};
